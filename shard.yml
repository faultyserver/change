name: change
version: 0.1.0

authors:
  - Jon <jonegeland@gmail.com>

crystal: 0.30.1

license: MIT

dependencies:
  # Ideally, this will get integrated to not be an explicit depdency.
  auto_initialize:
    github: kostya/auto_initialize


development_dependencies:
  pg:
    github: will/crystal-pg
